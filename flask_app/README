To run you should:

1) Make sure you've installed the requiremements (use pip to install requirements.txt, probably in a virtual env)

2) Set environment variable as follows:

    export FLASK_APP=run.py

(or whatever is the relative path to run.py in this repo.)

3) Finally

    flask run
