To run this flask app you need to get the models human_dog_net.pt and model_transfer.pt
and put them in app/neural_networks

You can generate those models in the jupyter notebook

Then run the app with
  flask run

I don't know how to deploy a web app and actually I just learned how to use flask!
